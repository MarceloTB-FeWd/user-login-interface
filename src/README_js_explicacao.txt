A numeração está no código

1 - Contempla da linha 2 a 4:

- Seleciona o primeiro elemento HTML5 com a classe "container" e o armazena na constante 'container'. 
Esse elemento será usado para adicionar ou remover uma classe no CSS mais tarde;

- Seleciona o primeiro elemento com a classe "register-btn" e o armazena na constante 'registerBtn'. 
Este elemento representa o botão de registro;

- Seleciona o primeiro elemento com a classe "login-btn" e o armazena na constante 'loginBtn'. 
Este elemento representa o botão de login.


2 - Contempla da 7 a 13:

- Adiciona um evento de clique ao 'registerBtn'. Quando o botão de registro é clicado, a classe 'active' é adicionada ao elemento "container".
Ativando os elementos no arquivo "style.css";

- Adiciona um evento de clique ao 'loginBtn'. Quando o botão de login é clicado, a classe 'active' é removida do elemento "container". 
Desativando os elementos do arquivo "style.css". 